version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - '6333:6333'   # HTTP API
      - '6334:6334'   # gRPC API (nécessaire pour Spring AI) 
    volumes:
      - ./qdrant_data:/qdrant/storage
    restart: unless-stopped
